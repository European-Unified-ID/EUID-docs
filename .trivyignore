# List any vulnerability that are to be accepted
# See https://aquasecurity.github.io/trivy/v0.35/docs/vulnerability/examples/filter/ 
# for more details
# e.g. 
# CVE-2022-3996

# CVE-2026-69152 — brace-expansion (npm) (HIGH).
# Not exploitable here: package.json overrides brace-expansion@5=5.0.8; docusaurus static-site
# generator (scripts: docusaurus build/start). Pulled transitively via minimatch/glob at build
# time only; production artifact is static HTML/JS with no expand() on runtime input.
# See: UID2-7619
CVE-2026-69152 exp:2026-09-06
